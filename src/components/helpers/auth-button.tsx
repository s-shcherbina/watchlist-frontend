import { Button, styled } from '@mui/material';

const AuthButton = styled(Button)({
  display: 'flex',
  alignSelf: 'center',
  width: '60%',
  borderRadius: '2.5rem',
  margin: '1rem',
});

export default AuthButton;
